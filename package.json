{
  "name": "portfolio-server",
  "version": "1.0.0",
  "description": "Backend server for portfolio contact form",
  "main": "server.js",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node --no-deprecation server.js",
    "dev": "node --no-deprecation server.js",
    "build": "echo 'Static portfolio - no build step needed'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^140.0.1",
    "nodemailer": "^6.9.7"
  }
}
